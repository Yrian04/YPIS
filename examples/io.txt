s = {}
r = false
a, b -> superset -> r, err:
    if a > b:
        r = a 
    elif a < b:
        r = b 
    else:
        r = {} 
        err = true
s, err -> output:
    if err:
        err -> writeln
    else:
        s -> writeln
-> read_set -> s1
-> read_set -> s2   
s1, s2 -> superset -> s, r
s, r -> output
